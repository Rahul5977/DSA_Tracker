export const DAYS = [
  {
    focus: 'Day 18 · High-yield interview round-up & rapid-fire',
    concepts: [
      {
        id: 'os-roundup-process-vs-thread-cheatsheet',
        topic: 'Process vs Thread cheat-sheet (concurrency vs parallelism)',
        summary: 'The single most asked OS interview question, distilled.',
        explanation:
          'A process is an independent program in execution with its own address space (code, data, heap, stack), file descriptors, and PCB. A thread is a lightweight unit of execution that lives inside a process; threads of the same process share the code, data (globals), heap, and open files, but each thread has its own stack, registers, and program counter. Because processes have separate address spaces, communication between them needs explicit IPC (pipes, shared memory, message queues, sockets), and a context switch between processes is expensive (the MMU/page tables and TLB must be reloaded). Threads share memory, so they communicate cheaply through shared variables, and switching between threads of the same process is much cheaper.\n\nThe trade-off: threads are fast and memory-efficient but fragile — one thread crashing or corrupting shared memory can bring down the whole process, and they require careful synchronization (mutexes, semaphores) to avoid race conditions. Processes are isolated and robust — one crashing does not affect others — at the cost of higher creation and communication overhead.\n\nConcurrency vs parallelism is the related conceptual question. Concurrency means multiple tasks make progress in overlapping time periods by interleaving (one CPU rapidly switching between tasks); it is about dealing with many things at once. Parallelism means multiple tasks literally execute at the same instant on multiple cores; it is about doing many things at once. Concurrency is possible on a single core; true parallelism requires multiple processing units. A concurrent design can run in parallel on a multi-core machine.',
        keyPoints: [
          'Process = own address space; thread = shares address space within a process',
          'Threads share code/data/heap/files; each has own stack, registers, PC',
          'Inter-process needs IPC; threads communicate via shared memory',
          'Process context switch is costlier (page tables + TLB flush) than thread switch',
          'Concurrency = interleaving (deal with many at once); parallelism = simultaneous execution on multiple cores',
        ],
        links: [
          { label: 'GeeksforGeeks', url: 'https://www.geeksforgeeks.org/difference-between-process-and-thread/' },
          { label: 'GeeksforGeeks', url: 'https://www.geeksforgeeks.org/difference-between-concurrency-and-parallelism/' },
        ],
        interview: [
          { q: 'What is the main difference between a process and a thread?', a: 'A process has its own separate address space; threads of a process share that address space but each keeps its own stack, registers, and program counter.' },
          { q: 'What do threads of the same process share and not share?', a: 'They share code, global/data segment, heap, and open file descriptors; they do not share stack, registers, or program counter.' },
          { q: 'Why is a thread switch cheaper than a process switch?', a: 'Threads share the same address space, so the page tables and TLB need not be flushed/reloaded — only registers and stack pointer change.' },
          { q: 'Concurrency vs parallelism?', a: 'Concurrency is interleaving multiple tasks so they make progress over overlapping intervals (possible on one core); parallelism is executing tasks literally simultaneously on multiple cores.' },
          { q: 'Can you have concurrency on a single-core CPU?', a: 'Yes — the scheduler time-slices and rapidly switches between tasks; you cannot have true parallelism with one core.' },
          { q: 'Why might a multi-threaded program be less robust than multi-process?', a: 'Threads share memory, so one thread can corrupt shared state or crash the whole process; processes are isolated so a crash is contained.' },
          { q: 'What is a thread sometimes called?', a: 'A lightweight process (LWP), because it carries less context than a full process.' },
          { q: 'When would you prefer multiple processes over multiple threads?', a: 'When isolation and fault tolerance matter (e.g., browser tabs, sandboxing) or when tasks need independent address spaces for security.' },
        ],
      },
      {
        id: 'os-roundup-special-processes',
        topic: 'Special processes: zombie, orphan, daemon & fork/exec/wait',
        summary: 'Zombies, orphans, daemons, fork() return values and the fork bomb.',
        explanation:
          'fork() creates a new child process by duplicating the calling (parent) process. After fork(), both parent and child continue from the same point. fork() returns twice: it returns the child PID (a positive value) to the parent, returns 0 to the child, and returns -1 on failure (no child created). This is how code distinguishes the two flows. exec() (execve and its family) replaces the current process image with a new program — same PID, new code/data — and does not return on success. The common pattern is fork() then exec() in the child to launch a new program while the parent keeps running. wait()/waitpid() makes the parent block until a child terminates and reaps the child status.\n\nA zombie process is a child that has terminated but whose entry still lingers in the process table because the parent has not yet called wait() to read its exit status. The zombie holds no memory or CPU — just a PCB entry — but too many zombies can exhaust the process table. The fix is for the parent to wait()/reap; if the parent dies, init/systemd adopts and reaps the zombie.\n\nAn orphan process is a child whose parent terminated before it. Orphans are immediately adopted by init (PID 1, or systemd), which becomes their new parent and eventually reaps them — so orphans are harmless. A daemon is a deliberately created long-running background process with no controlling terminal (e.g., sshd, cron). A daemon is typically created by forking, having the parent exit (so the child becomes an orphan adopted by init), calling setsid() to detach from the terminal, and redirecting standard streams. A fork bomb is a denial-of-service where a process repeatedly forks itself (e.g., :(){ :|:& };:) until the process table and resources are exhausted, freezing the system.',
        keyPoints: [
          'fork() returns child PID to parent, 0 to child, -1 on failure (returns twice)',
          'exec() replaces the process image (same PID); does not return on success',
          'wait()/waitpid() blocks the parent until a child exits and reaps its status',
          'Zombie = terminated child not yet reaped (occupies a process-table slot only)',
          'Orphan = child whose parent died; adopted by init/systemd (PID 1)',
          'Daemon = detached background process (fork + setsid, no controlling terminal)',
          'Fork bomb = runaway self-forking that exhausts the process table',
        ],
        links: [
          { label: 'GeeksforGeeks', url: 'https://www.geeksforgeeks.org/zombie-and-orphan-processes-in-c/' },
          { label: 'GeeksforGeeks', url: 'https://www.geeksforgeeks.org/fork-system-call/' },
        ],
        interview: [
          { q: 'What does fork() return?', a: 'The child PID (>0) to the parent, 0 to the child, and -1 on failure. It effectively returns twice.' },
          { q: 'What is a zombie process and how do you avoid it?', a: 'A child that has terminated but is not yet reaped by the parent, leaving a process-table entry. Avoid it by having the parent call wait()/waitpid().' },
          { q: 'What is an orphan process?', a: 'A process whose parent terminated before it; it is adopted by init/systemd (PID 1), which reaps it. Orphans are harmless.' },
          { q: 'How is a daemon process created?', a: 'Typically fork, let the parent exit so the child is orphaned and reparented to init, call setsid() to start a new session with no controlling terminal, then redirect stdin/stdout/stderr.' },
          { q: 'Difference between fork() and exec()?', a: 'fork() duplicates the process to create a child (two processes); exec() replaces the current process image with a new program (same process, new code).' },
          { q: 'What is a fork bomb?', a: 'A process that recursively forks itself without limit, exhausting the process table and system resources — a denial-of-service.' },
          { q: 'Does a zombie consume CPU or memory?', a: 'No — it has released its memory and CPU; only its PCB/process-table entry remains until reaped.' },
          { q: 'What happens to a zombie if its parent never calls wait() and then dies?', a: 'The zombie is reparented to init/systemd, which reaps it automatically.' },
        ],
      },
      {
        id: 'os-roundup-address-translation-recap',
        topic: 'Address translation recap: virtual vs physical, 32-bit vs 64-bit',
        summary: 'Why virtual memory exists and how address space size is computed.',
        explanation:
          'A virtual (logical) address is the address generated by the CPU/program; a physical address is the actual location in RAM. The Memory Management Unit (MMU) translates virtual to physical addresses at runtime, usually via page tables. Programs only ever see virtual addresses; this indirection is the foundation of virtual memory.\n\nVirtual memory gives each process the illusion of a large, private, contiguous address space even though physical RAM is smaller and shared. Its benefits: isolation/protection between processes, the ability to run programs larger than physical RAM (by paging out to disk/swap), simpler relocation and linking, and efficient memory sharing (e.g., shared libraries mapped into many processes). The cost is translation overhead, mitigated by the TLB (a cache of recent translations).\n\nAddress space size follows directly from address width: an n-bit address can address 2^n bytes. So a 32-bit address space spans 2^32 bytes = 4 GB, which is why 32-bit systems are limited to ~4 GB of addressable memory per process. A 64-bit address space is 2^64 bytes = 16 exabytes in theory, though real CPUs implement fewer bits (commonly 48), still giving 256 TB — effectively unlimited for today.\n\nPaging recap: virtual and physical memory are divided into fixed-size pages and frames. The page table maps page numbers to frame numbers; a virtual address splits into a page number (index into the page table) and an offset (within the page). On access, the MMU checks the TLB first; a TLB hit translates quickly, a TLB miss walks the page table, and if the page is not in RAM a page fault brings it in from disk.',
        keyPoints: [
          'Virtual/logical address = CPU-generated; physical address = actual RAM location',
          'MMU translates virtual to physical (via page tables); TLB caches translations',
          'Virtual memory gives isolation, larger-than-RAM programs, easy relocation/sharing',
          'n-bit address => 2^n bytes: 32-bit = 4 GB, 64-bit = 2^64 (often 48-bit = 256 TB in practice)',
          'Virtual address = page number + offset; page table maps page -> frame',
        ],
        links: [
          { label: 'GeeksforGeeks', url: 'https://www.geeksforgeeks.org/virtual-memory-in-operating-system/' },
          { label: 'GeeksforGeeks', url: 'https://www.geeksforgeeks.org/logical-and-physical-address-in-operating-system/' },
        ],
        interview: [
          { q: 'Difference between logical/virtual and physical address?', a: 'The virtual address is generated by the CPU/program; the physical address is the real location in RAM. The MMU maps virtual to physical at runtime.' },
          { q: 'Why do we need virtual memory?', a: 'For process isolation/protection, to run programs larger than physical RAM via paging to disk, and to simplify relocation and memory sharing.' },
          { q: 'How much memory can a 32-bit system address?', a: '2^32 bytes = 4 GB per process, because a 32-bit address can name 4 billion distinct byte locations.' },
          { q: 'How big is a 64-bit address space?', a: 'Theoretically 2^64 bytes (16 EB); real CPUs commonly implement 48 address bits, giving 256 TB.' },
          { q: 'What does the MMU do?', a: 'It translates virtual addresses to physical addresses (typically using page tables) and enforces memory protection.' },
          { q: 'What is the TLB and why is it needed?', a: 'The Translation Lookaside Buffer caches recent virtual-to-physical translations so the MMU can skip the slow page-table walk on a hit.' },
          { q: 'How is a virtual address broken down in paging?', a: 'Into a page number (used to index the page table) and an offset within the page; the page table yields the frame number, combined with the offset to form the physical address.' },
        ],
      },
      {
        id: 'os-roundup-sync-recap',
        topic: 'Synchronization recap: mutex, semaphore, monitor, deadlock',
        summary: 'Locks compared, plus deadlock vs starvation vs livelock.',
        explanation:
          'A mutex (mutual exclusion lock) is a locking mechanism owned by exactly one thread at a time; only the thread that locked it can unlock it. It is used to protect a critical section so just one thread enters at a time. A semaphore is a signaling mechanism — an integer counter with atomic wait (P/down) and signal (V/up) operations. A binary semaphore (0/1) resembles a mutex but has no ownership concept (any thread can signal it), while a counting semaphore allows up to N threads into a resource pool. Key distinction: a mutex is a locking primitive with ownership; a semaphore is a signaling primitive without ownership.\n\nA monitor is a higher-level synchronization construct (provided by languages like Java via synchronized, or C#) that bundles shared data with the procedures that operate on it and guarantees that only one thread executes inside the monitor at a time, using condition variables (wait/notify) for coordination. It is safer and less error-prone than raw semaphores because mutual exclusion is automatic.\n\nA spinlock is a lock where a thread waiting for it loops (busy-waits) repeatedly checking the lock instead of sleeping. It avoids context-switch overhead and is efficient when the wait is very short and on multiprocessor systems, but wastes CPU if held for long.\n\nDeadlock occurs when a set of processes are blocked forever, each holding a resource and waiting for another held by the next. The four Coffman conditions, all required simultaneously, are: mutual exclusion, hold and wait, no preemption, and circular wait. Starvation is when a process waits indefinitely because resources keep going to others (e.g., low priority); unlike deadlock no circular wait exists, and it can resolve (e.g., via aging). Livelock is when processes keep changing state in response to each other but make no progress (like two people repeatedly stepping aside in a corridor) — they are not blocked, just stuck.',
        keyPoints: [
          'Mutex = locking with ownership (only locker unlocks); one thread in critical section',
          'Semaphore = signaling counter (P/wait, V/signal); binary vs counting; no ownership',
          'Monitor = high-level construct with automatic mutual exclusion + condition variables',
          'Spinlock = busy-wait lock; good for very short waits / multiprocessors',
          'Deadlock needs all 4 Coffman conditions: mutual exclusion, hold & wait, no preemption, circular wait',
          'Deadlock = stuck blocked; starvation = waits indefinitely (no circular wait); livelock = active but no progress',
        ],
        links: [
          { label: 'GeeksforGeeks', url: 'https://www.geeksforgeeks.org/mutex-vs-semaphore/' },
          { label: 'GeeksforGeeks', url: 'https://www.geeksforgeeks.org/deadlock-starvation-and-livelock/' },
        ],
        interview: [
          { q: 'Mutex vs semaphore?', a: 'A mutex is a locking mechanism with ownership (only the locking thread unlocks it); a semaphore is a signaling counter with no ownership that any thread can signal.' },
          { q: 'Binary semaphore vs mutex?', a: 'Both allow one accessor, but a mutex enforces ownership (only the locker releases), whereas any thread can signal a binary semaphore — semaphores are for signaling, mutexes for locking.' },
          { q: 'What is a monitor?', a: 'A high-level synchronization construct that encapsulates shared data with its operations and guarantees automatic mutual exclusion, using condition variables (wait/notify) for coordination.' },
          { q: 'What is a spinlock and when is it useful?', a: 'A lock where a waiter busy-waits in a loop instead of sleeping; useful when the critical section is very short and on multiprocessor systems, avoiding context-switch cost.' },
          { q: 'What are the four conditions for deadlock?', a: 'Mutual exclusion, hold and wait, no preemption, and circular wait — all four must hold at once.' },
          { q: 'Difference between deadlock and starvation?', a: 'In deadlock, processes are permanently blocked in a circular wait; in starvation a process is repeatedly denied resources but no circular wait exists and it may eventually proceed (e.g., via aging).' },
          { q: 'What is livelock?', a: 'Processes keep responding to each other and changing state but make no real progress — they are not blocked, just spinning uselessly.' },
          { q: 'What is aging in scheduling/synchronization?', a: 'Gradually increasing the priority of a waiting process so it eventually runs, preventing indefinite starvation.' },
        ],
      },
      {
        id: 'os-roundup-misc-high-yield',
        topic: 'Misc high-yield: system calls, interrupts, modes, OS types',
        summary: 'System vs library calls, interrupt vs trap, modes, RTOS, OS multitasking flavors.',
        explanation:
          'A system call is the programmatic interface by which a user process requests a service from the OS kernel (e.g., read, write, fork, open). It causes a switch from user mode to kernel mode. A library call / API call is an ordinary function call into a library that runs in user space; it may internally invoke one or more system calls (e.g., printf is a library function that eventually calls the write system call) but does not by itself switch to the kernel.\n\nKernel mode vs user mode are two CPU privilege levels enforced by hardware. In kernel (supervisor) mode the code can execute privileged instructions and access all hardware and memory; in user mode the process is restricted and must request privileged operations via system calls. This dual-mode operation protects the OS and other processes from a misbehaving program.\n\nAn interrupt is an asynchronous signal, usually from hardware (timer, I/O completion, keyboard), that diverts the CPU to an interrupt handler. A trap (or exception) is synchronous and software-generated — caused by the executing instruction itself, such as a division by zero, a page fault, or a deliberate system-call instruction. Roughly: interrupts are external/asynchronous, traps/exceptions are internal/synchronous.\n\nScheduling can be preemptive (the OS can forcibly take the CPU from a running process, e.g., Round Robin, preemptive SJF/SRTF, priority) or non-preemptive (a process keeps the CPU until it blocks or finishes, e.g., FCFS, non-preemptive SJF). OS execution models: multiprogramming keeps several jobs in memory so the CPU always has work (overlaps CPU with I/O); multitasking (time-sharing) rapidly switches the CPU among tasks to give interactive responsiveness; multiprocessing uses multiple CPUs/cores to run processes truly in parallel. A real-time OS (RTOS) guarantees responses within deadlines: a hard real-time system must never miss a deadline (e.g., pacemaker, airbag controller), while a soft real-time system tries to meet deadlines but tolerates occasional misses with degraded quality (e.g., video streaming).',
        keyPoints: [
          'System call = request to kernel (user->kernel mode switch); library/API call runs in user space and may wrap system calls',
          'Kernel mode = privileged/full access; user mode = restricted; hardware-enforced dual mode protects the OS',
          'Interrupt = asynchronous, external (hardware); trap/exception = synchronous, internal (instruction-caused)',
          'Preemptive = OS can take CPU away (RR, SRTF); non-preemptive = process holds CPU till blocks/exits (FCFS)',
          'Multiprogramming (keep CPU busy) vs multitasking (time-sharing responsiveness) vs multiprocessing (multiple CPUs)',
          'Hard RTOS must never miss a deadline; soft RTOS tolerates occasional misses',
        ],
        links: [
          { label: 'GeeksforGeeks', url: 'https://www.geeksforgeeks.org/introduction-of-system-call/' },
          { label: 'GeeksforGeeks', url: 'https://www.geeksforgeeks.org/difference-between-trap-and-interrupt/' },
        ],
        interview: [
          { q: 'System call vs library/API call?', a: 'A system call requests a service from the kernel and switches to kernel mode; a library call is a user-space function that may internally make system calls but does not itself enter the kernel.' },
          { q: 'What is the difference between user mode and kernel mode?', a: 'Kernel mode can run privileged instructions and access all hardware/memory; user mode is restricted and must use system calls for privileged operations. Hardware enforces this for protection.' },
          { q: 'Interrupt vs trap/exception?', a: 'An interrupt is asynchronous and external (hardware-generated); a trap/exception is synchronous and generated by the running instruction (e.g., divide-by-zero, page fault, system call).' },
          { q: 'Preemptive vs non-preemptive scheduling?', a: 'Preemptive scheduling can forcibly remove a process from the CPU (Round Robin, SRTF); non-preemptive lets a process run until it blocks or finishes (FCFS, non-preemptive SJF).' },
          { q: 'Multiprogramming vs multitasking vs multiprocessing?', a: 'Multiprogramming keeps several jobs in memory to keep the CPU busy; multitasking time-shares the CPU for interactive responsiveness; multiprocessing uses multiple CPUs/cores for true parallel execution.' },
          { q: 'What is a real-time OS, and hard vs soft?', a: 'An RTOS guarantees task completion within deadlines. Hard real-time must never miss a deadline (pacemaker, airbag); soft real-time tolerates occasional misses with degraded performance (video streaming).' },
          { q: 'Give an example of a synchronous vs asynchronous event.', a: 'A page fault or divide-by-zero is synchronous (trap, caused by the instruction); an I/O completion or timer tick is asynchronous (interrupt, external).' },
          { q: 'Why is dual-mode operation important?', a: 'It prevents user programs from directly executing privileged instructions or corrupting the OS and other processes, providing protection and isolation.' },
        ],
      },
      {
        id: 'os-roundup-scheduler-types',
        topic: 'Schedulers: long/medium/short-term & the dispatcher',
        summary: 'The three schedulers, the dispatcher, dispatch latency and degree of multiprogramming.',
        explanation:
          'Operating systems use up to three types of schedulers. The long-term scheduler (job scheduler) decides which jobs are admitted from the job pool into the ready queue (into memory). It runs infrequently and controls the degree of multiprogramming — the number of processes in memory at once. The short-term scheduler (CPU scheduler) selects which ready process gets the CPU next; it runs very frequently (milliseconds) and must be fast. The medium-term scheduler handles swapping — moving processes out of memory (suspend) to disk and back in to relieve memory pressure or improve the process mix; it reduces the degree of multiprogramming temporarily.\n\nThe dispatcher is the module that actually gives CPU control to the process selected by the short-term scheduler. Its job includes switching context, switching to user mode, and jumping to the proper location in the program to restart it. Dispatch latency is the time the dispatcher takes to stop one process and start another. It should be minimal because it is pure overhead.\n\nA common point of confusion: the scheduler decides which process to run (a policy/decision), while the dispatcher carries out that decision (the mechanism of actually loading and starting it). The degree of multiprogramming is how many processes are resident in memory; the long-term scheduler primarily controls it, and the medium-term scheduler adjusts it via swapping.',
        keyPoints: [
          'Long-term (job) scheduler: admits jobs into memory; controls degree of multiprogramming; runs rarely',
          'Short-term (CPU) scheduler: picks the next ready process for the CPU; runs very often; must be fast',
          'Medium-term scheduler: swaps processes in/out of memory to manage the process mix and memory',
          'Dispatcher: actually hands the CPU to the chosen process (context switch, switch to user mode, jump)',
          'Dispatch latency = time to stop one process and start another (overhead, minimize it)',
          'Scheduler decides (policy); dispatcher does (mechanism)',
        ],
        links: [
          { label: 'GeeksforGeeks', url: 'https://www.geeksforgeeks.org/operating-system-process-schedulers/' },
          { label: 'GeeksforGeeks', url: 'https://www.geeksforgeeks.org/difference-between-dispatcher-and-scheduler/' },
        ],
        interview: [
          { q: 'What are the three types of schedulers?', a: 'Long-term (job) scheduler admits jobs into memory; short-term (CPU) scheduler picks the next process to run; medium-term scheduler swaps processes in and out of memory.' },
          { q: 'Which scheduler controls the degree of multiprogramming?', a: 'The long-term scheduler primarily controls it by deciding how many jobs enter memory; the medium-term scheduler adjusts it via swapping.' },
          { q: 'Difference between scheduler and dispatcher?', a: 'The scheduler decides which process should run (policy); the dispatcher actually gives the CPU to that process by performing the context switch and mode change (mechanism).' },
          { q: 'What is dispatch latency?', a: 'The time the dispatcher takes to stop the currently running process and start the next selected one — pure overhead that should be minimized.' },
          { q: 'Why must the short-term scheduler be fast?', a: 'Because it runs very frequently (every few milliseconds); any time it spends is overhead taken from useful CPU work.' },
          { q: 'What does the medium-term scheduler do?', a: 'It swaps processes out to disk and back to relieve memory pressure and improve the mix of CPU-bound and I/O-bound processes.' },
        ],
      },
      {
        id: 'os-roundup-rapid-fire',
        topic: 'Rapid-fire: classic OS one-liners',
        summary: 'The greatest-hits list of fast OS interview Q&A.',
        explanation:
          'This is a rapid-fire drill of the most frequently asked one-line OS interview questions. The goal is instant, confident recall — interviewers often fire several of these in a row to gauge fundamentals before going deeper. Read each question, answer in your head in one sentence, then check against the model answer.\n\nFocus on precision: small distinctions like turnaround vs response vs waiting time, internal vs external fragmentation, or the difference between a page fault and thrashing are exactly what separates a crisp candidate from a vague one. If you can answer all of these in a single breath each, your OS basics are interview-ready.',
        keyPoints: [
          'Answer each in one sentence — aim for instant recall',
          'Know the subtle pairs: turnaround vs waiting, internal vs external fragmentation, page fault vs thrashing',
          'These come up across both service and product company interviews',
        ],
        links: [
          { label: 'GeeksforGeeks', url: 'https://www.geeksforgeeks.org/last-minute-notes-operating-systems/' },
          { label: 'GeeksforGeeks', url: 'https://www.geeksforgeeks.org/commonly-asked-operating-systems-interview-questions-set-1/' },
        ],
        interview: [
          { q: 'Throughput vs turnaround time?', a: 'Throughput is the number of processes completed per unit time; turnaround time is the total time from submission to completion of one process.' },
          { q: 'What is turnaround vs waiting vs response time?', a: 'Turnaround = completion - arrival; waiting = turnaround - burst (time spent in ready queue); response = first time the process gets the CPU - arrival.' },
          { q: 'What is a context switch and is it useful work?', a: 'Saving the state of one process and loading another so the CPU can switch between them; it is pure overhead, not useful work.' },
          { q: 'What is demand paging?', a: 'Loading a page into memory only when it is actually referenced (on a page fault), rather than loading the whole program upfront.' },
          { q: 'What is thrashing?', a: 'When a system spends most of its time paging (swapping pages in/out) rather than executing, due to too high a degree of multiprogramming and too little memory.' },
          { q: 'What is a page fault?', a: 'A trap that occurs when a process accesses a page that is not currently in physical memory, prompting the OS to fetch it from disk.' },
          { q: 'What is the TLB?', a: 'The Translation Lookaside Buffer — a cache of recent virtual-to-physical address translations that speeds up paging by avoiding page-table walks.' },
          { q: 'What is Belady\'s anomaly?', a: 'A counter-intuitive case where increasing the number of page frames increases the number of page faults; it occurs with FIFO page replacement (not with LRU/optimal).' },
          { q: 'Internal vs external fragmentation?', a: 'Internal fragmentation is wasted space inside an allocated block (e.g., unused bytes in a page); external fragmentation is free memory split into small non-contiguous holes that cannot satisfy a request.' },
          { q: 'Which suffers internal vs external fragmentation: paging or segmentation?', a: 'Paging suffers internal fragmentation (fixed-size pages); segmentation (and contiguous allocation) suffers external fragmentation (variable-size holes).' },
          { q: 'Dispatcher vs scheduler?', a: 'The scheduler decides which process runs next (policy); the dispatcher actually hands it the CPU via a context switch (mechanism).' },
          { q: 'Name the three scheduler types.', a: 'Long-term (job) scheduler, short-term (CPU) scheduler, and medium-term scheduler (swapping).' },
          { q: 'What is the purpose of the Banker\'s algorithm?', a: 'It is a deadlock-avoidance algorithm that grants a resource request only if the system stays in a safe state where all processes can still finish.' },
          { q: 'What is RAID (briefly)?', a: 'Redundant Array of Independent Disks — combining multiple disks for performance and/or redundancy (e.g., RAID 0 = striping, RAID 1 = mirroring, RAID 5 = striping with distributed parity).' },
          { q: 'Cache vs buffer vs spooling?', a: 'A cache stores frequently accessed data for fast reuse; a buffer temporarily holds data while transferring between devices of different speeds; spooling queues jobs (e.g., print jobs) to a disk so a slow device can process them at its own pace.' },
          { q: 'What is busy waiting?', a: 'A process repeatedly checks a condition in a loop (spinning) instead of sleeping, wasting CPU cycles while it waits.' },
          { q: 'What is a critical section?', a: 'A code region that accesses shared resources and must be executed by only one process/thread at a time to avoid race conditions.' },
          { q: 'What is a race condition?', a: 'A bug where the outcome depends on the unpredictable timing/interleaving of concurrent threads accessing shared data.' },
          { q: 'What is a semaphore in one line?', a: 'An integer counter with atomic wait (P) and signal (V) operations used to control access to shared resources.' },
          { q: 'What is the difference between paging and segmentation?', a: 'Paging divides memory into fixed-size pages (no semantic meaning, causes internal fragmentation); segmentation divides it into variable-size logical segments like code/stack/data (causes external fragmentation).' },
          { q: 'What is the convoy effect?', a: 'In FCFS scheduling, short processes get stuck waiting behind a long-running process, hurting overall throughput.' },
        ],
      },
    ],
  },
]
